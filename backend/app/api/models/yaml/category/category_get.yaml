Получить список всех категорий
---
tags:
  - User option, work with items
security:
  - bearerAuth: [ ]
responses:
  200:
    description: List of categories are given.
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/ListCategoryResponses'