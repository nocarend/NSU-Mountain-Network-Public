Смена номера телефона
---
tags:
  - User's personal data
security:
  - bearerAuth: [ ]
requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/NewPhoneRequest'
      required: 'true'
responses:
  200:
    description: Phone has changed
  403:
    description: Number format is wrong or phone is already taken