Отправить на почту пользователя письмо для восстановления пароля
---
tags:
  - Authorization, authentication, etc
requestBody:
  content:
    application/json:
      schema:
        $ref: '#/components/schemas/EmailRequest'
responses:
  200:
    description: Mail is send.
  403:
    description: User is already authenticated or Email doesn't exist.
